class Solution {
    public void reverseString(char[] s) {
        int len=s.length;
        int p=0,q=len-1;
        while(p<q){
            char temp=s[p];
            s[p]=s[q];
            s[q]=temp;
            p++;
            q--;
        }
    }
}
